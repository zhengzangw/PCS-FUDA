# setup.py
#!/usr/bin/env python

from setuptools import find_packages, setup

setup(
    name="pcs",
    version="1.0.0",
    description="This is pytorch implementation of Prototypical Cross-domain Self-supervised network (PCS)",
    packages=["pcs"],
)
